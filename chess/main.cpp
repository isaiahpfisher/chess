#include "header.h"

int main() {

    vector<int> nums = { 2, 9, 29, 72 };

    for (int i = 0; i < nums.size(); i++) {
        cout << nums[i] << endl;
    }
    
    cout << "WASSUP!";

    return 0;
}